namespace :flight_pdf do
  # desc 'Generates report with flights'
  # task :generate_pdf => :environment do
  #   output = FlightPdf.new.to_pdf
  #   filename = "flight_report.pdf"
  #   File.open(Rails.root.join('public', filename), 'wb') do |f|
  #     f.write(output)
  #   end
  #   puts "Report was written to #{filename}"
  # end
end
